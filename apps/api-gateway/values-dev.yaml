replicaCount: 1

image:
  repository: "REPLACE_WITH_ECR_REPO_URL/api-gateway"
  tag: "latest"

autoscaling:
  enabled: false

ingress:
  enabled: true
  host: "dev.api.example.com"

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 150m
    memory: 128Mi
